# kern

Open-source shear wall designer — educational and design calculations per **ACI 318-19**.

## Structure

- `apps/web` — Next.js app (UI shell: shadcn/ui + Tailwind 4)
- `packages/` — shared packages (calc engine, etc.)

## Development

```sh
pnpm install
pnpm dev
```
